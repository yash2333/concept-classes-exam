const app=document.getElementById("app"),socket=io();
let S={user:null,attemptId:null,test:null,questions:[],violations:0,submitted:false,monitor:null,timer:null,role:"student"};
async function api(u,o={}){let r=await fetch(u,o),d=await r.json();if(!r.ok)throw Error(d.error||"Something went wrong");return d}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}
async function boot(){try{S.user=(await api("/api/me")).user}catch(e){S.user=null}S.user?(S.user.role==="admin"||S.user.role==="teacher"?teacher():student()):login()}
function brand(dark=true){return `<div class="brand"><img class="brand-logo-img" src="/concept-classes-logo.jpg" alt="Concept Classes logo"><div><span class="brand-name">Concept Classes</span><small>Online Examination Portal</small></div></div>`}
function nav(t){return `<div class="nav">${brand()}<div class="nav-right"><span class="nav-title">${esc(t)}</span><div class="user-chip">${S.user?.role==="admin"||S.user?.role==="teacher"?"Teacher":"Student"} · ${esc(S.user?.username||"")}</div><button class="btn ghost" onclick="logout()">Logout</button></div></div>`}
function login(){S.role="student";app.innerHTML=`<div class="login-shell"><section class="login-brand"><div class="login-copy"><div class="eyebrow"><i></i> Secure digital examination</div><div class="login-brand-lockup"><img src="/concept-classes-logo.jpg" class="login-logo" alt="Concept Classes"><div><strong>CONCEPT CLASSES</strong><small>Success Through Understanding</small></div></div><h1>Learn. Practice.<br><span>Perform.</span></h1><p>A professional examination portal built for Concept Classes — with timed tests, secure sessions and supervised written-paper evaluation.</p><div class="feature-row"><span class="feature">Timed examinations</span><span class="feature">Secure proctoring</span><span class="feature">Written solutions</span><span class="feature">Teacher evaluation</span></div><img class="login-poster" src="/concept-classes-poster.jpg" alt="Concept Classes branding"></div></section><section class="login-panel"><div class="login-box">${brand(false)}<h2>Welcome back</h2><p class="muted" style="margin-top:0">Choose your portal to continue.</p><div class="role-grid"><div id="studentRole" class="role-card active" onclick="selectRole('student')"><div class="check">✓</div><div class="role-icon">🎓</div><strong>Student</strong><small>Take your assigned examinations</small></div><div id="teacherRole" class="role-card" onclick="selectRole('teacher')"><div class="check">✓</div><div class="role-icon">🧑‍🏫</div><strong>Teacher</strong><small>Manage tests and review results</small></div></div><label class="label">Username</label><input id="u" class="input" autocomplete="username" placeholder="Enter your username"><label class="label">Password</label><input id="p" type="password" class="input" autocomplete="current-password" placeholder="Enter your password" onkeydown="if(event.key==='Enter')doLogin()"><button class="btn full" onclick="doLogin()">Sign in securely</button><div class="login-note">Use the account credentials provided by your institute. For security, login credentials are not displayed on this portal.</div><div class="login-footer">© ${new Date().getFullYear()} Concept Classes · <b>Designed & Developed by Yash Pandey</b></div></div></section></div>`}
function selectRole(role){S.role=role;document.getElementById("studentRole").classList.toggle("active",role==="student");document.getElementById("teacherRole").classList.toggle("active",role==="teacher")}
async function doLogin(){let username=document.getElementById("u").value.trim(),password=document.getElementById("p").value;if(!username||!password)return alert("Please enter your username and password.");try{await api("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password,role:S.role})});boot()}catch(e){alert(e.message)}}
async function logout(){await api("/api/logout",{method:"POST"});location.reload()}
async function teacher(){let[ts,rs]=await Promise.all([api("/api/admin/tests"),api("/api/admin/results")]);app.innerHTML=nav("Teacher Dashboard")+`<div class="wrap"><div class="top-banner"><h1 class="hero-title">Teacher Dashboard</h1><p class="muted" style="margin:0">Create examinations, add questions, review papers and publish results.</p></div><div class="stats"><div class="stat"><div class="stat-label">Examinations</div><div class="stat-value">${ts.length}</div></div><div class="stat"><div class="stat-label">Questions</div><div class="stat-value">${ts.reduce((n,t)=>n+t.questions.length,0)}</div></div><div class="stat"><div class="stat-label">Submissions</div><div class="stat-value">${rs.length}</div></div><div class="stat"><div class="stat-label">Published Results</div><div class="stat-value">${rs.filter(r=>r.result_published).length}</div></div></div><div class="card"><div class="section-head"><h2>Create Student Account</h2><span class="badge blue">Teacher access</span></div><div class="field-row"><div><label class="label">Student username</label><input id="su" class="input" placeholder="e.g. student01"></div><div><label class="label">Temporary password</label><input id="sp" type="password" class="input" placeholder="Set a secure password"></div></div><button class="btn" onclick="mkStudent()">Create Student</button></div><div class="card"><div class="section-head"><h2>Create Examination</h2></div><div class="field-row three"><div><label class="label">Test title</label><input id="tt" class="input" placeholder="Mathematics Unit Test"></div><div><label class="label">Subject</label><input id="sub" class="input" placeholder="Mathematics"></div><div><label class="label">Class</label><input id="cl" class="input" placeholder="10"></div></div><div class="field-row"><div><label class="label">Duration (minutes)</label><input id="dur" type="number" value="30" min="1" class="input"></div><div style="display:flex;align-items:end"><button class="btn" onclick="mkTest()" style="margin-bottom:13px">Create Examination</button></div></div></div><div class="card"><div class="section-head"><h2>Add Question</h2><span class="badge amber">MCQ + Written</span></div><div class="field-row"><div><label class="label">Examination</label><select id="tsel" class="input" style="color:#111827;background:#fff" ${ts.length?"":"disabled"}>${ts.length?`<option value="" disabled>Select an examination</option>${ts.map(t=>`<option value="${t.id}">${esc(t.title||(`Examination ${t.id}`))}</option>`).join("")}`:`<option value="">No examinations found — create one above</option>`}</select><div id="testSelectHelp" class="mini" style="margin-top:-8px;margin-bottom:10px">${ts.length?`${ts.length} examination${ts.length===1?"":"s"} available.`:"Create an examination first, then return here."}</div></div><div><label class="label">Question type</label><select id="qt" class="input" onchange="toggleQ()"><option value="mcq">Multiple Choice (MCQ)</option><option value="written">Long Question / Written Solution</option></select></div></div><label class="label">Question</label><textarea id="qq" class="input" placeholder="Enter the question here..."></textarea><div id="opts"><div class="field-row"><input id="qa" class="input" placeholder="Option A"><input id="qb" class="input" placeholder="Option B"></div><div class="field-row"><input id="qc" class="input" placeholder="Option C"><input id="qd" class="input" placeholder="Option D"></div><label class="label">Correct option</label><select id="cr" class="input"><option>A</option><option>B</option><option>C</option><option>D</option></select></div><div class="field-row three"><div><label class="label">Marks</label><input id="mk" type="number" value="1" class="input"></div><div><label class="label">Negative marks</label><input id="neg" type="number" value="0" step=".25" class="input"></div><div><label class="label">Written upload deadline (minutes)</label><input id="uw" type="number" value="15" class="input"></div></div><button class="btn" onclick="addQ()">Add Question</button></div><div class="card"><div class="section-head"><div><h2>Paper Checking</h2><p class="mini">Open submitted written papers, award marks, add comments and publish results.</p></div><button class="btn" onclick="paperReview()">Check Student Papers →</button></div></div><div class="card"><div class="section-head"><h2>Examinations</h2></div>${ts.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Examination</th><th>Subject</th><th>Class</th><th>Duration</th><th>Questions</th></tr></thead><tbody>${ts.map(t=>`<tr><td><b>${esc(t.title)}</b></td><td>${esc(t.subject)}</td><td>${esc(t.class_name)}</td><td>${t.duration} min</td><td><span class="badge blue">${t.questions.length}</span></td></tr>`).join("")}</tbody></table></div>`:`<div class="empty">No examinations created yet.</div>`}</div><div class="card"><div class="section-head"><h2>Student Results</h2><span class="badge green">Check and publish before students can view</span></div>${rs.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Student</th><th>Examination</th><th>Score</th><th>Status</th><th>Action</th></tr></thead><tbody>${rs.map(r=>`<tr><td>${esc(r.username)}</td><td>${esc(r.title)}</td><td><b>${r.score}/${r.total}</b></td><td><span class="badge ${r.result_published?'green':'amber'}">${r.result_published?'Published':(r.review_status==='checked'?'Checked':'Pending review')}</span></td><td><button class="btn ghost" onclick="reviewAttempt(${r.id})">${r.result_published?'View':'Check Paper'}</button></td></tr>`).join("")}</tbody></table></div>`:`<div class="empty">No submissions yet.</div>`}</div><div class="footer">Concept Classes · Professional Examination Portal · Designed & Developed by Yash Pandey</div></div>`}
async function paperReview(){try{let papers=await api("/api/admin/papers");app.innerHTML=nav("Written Paper Review")+`<div class="wrap"><div class="top-banner"><h1 class="hero-title">Check Student Papers</h1><p class="muted" style="margin:0">Award marks question-by-question, add teacher remarks and publish the final result.</p></div>${papers.length?papers.map(p=>`<div class="card"><div class="section-head"><div><h2>${esc(p.username)} · ${esc(p.title)}</h2><p class="mini">Submitted ${new Date(p.submittedAt).toLocaleString()} · Current score ${p.score}/${p.total}</p></div><span class="badge ${p.resultPublished?'green':p.reviewStatus==='checked'?'blue':'amber'}">${p.resultPublished?'Published':p.reviewStatus==='checked'?'Checked':'Pending Review'}</span></div><button class="btn" onclick="reviewAttempt(${p.attemptId})">Open Paper & Mark →</button></div>`).join(""):`<div class="card"><div class="empty">No submitted papers are available yet.</div></div>`}<button class="btn ghost" onclick="teacher()">← Back to Teacher Dashboard</button><div class="footer">Concept Classes · Written Paper Review · Designed & Developed by Yash Pandey</div></div>`}catch(e){alert(e.message)}}
async function reviewAttempt(id){try{let p=await api(`/api/admin/papers/${id}`);app.innerHTML=nav("Check Student Paper")+`<div class="wrap"><div class="top-banner"><h1 class="hero-title">${esc(p.username)} · ${esc(p.title)}</h1><p class="muted" style="margin:0">MCQ score: ${p.mcqScore}/${p.total} · Review the written answers below.</p></div><div class="card"><div class="section-head"><h2>Written Answers</h2><span class="badge ${p.resultPublished?'green':'amber'}">${p.resultPublished?'Result Published':'Not Published'}</span></div>${p.questions.length?p.questions.map((q,i)=>`<div class="paper-item"><div class="question-number">Question ${i+1} · Maximum ${q.marks} marks</div><div class="question-title">${esc(q.question)}</div>${q.imageUrl?`<div class="paper-image-wrap"><img class="paper-image" src="${q.imageUrl}" alt="Student written solution"><a class="btn ghost" href="${q.imageUrl}" target="_blank">Open full image</a></div>`:`<div class="empty small">No solution image was uploaded.</div>`}<div class="field-row" style="margin-top:16px"><div><label class="label">Marks awarded</label><input class="input grade-mark" data-qid="${q.questionId}" data-max="${q.marks}" type="number" min="0" max="${q.marks}" step="0.5" value="${q.awardedMarks??""}" ${p.resultPublished?'disabled':''}></div><div><label class="label">Teacher comment</label><input class="input grade-comment" data-qid="${q.questionId}" value="${esc(q.comment)}" placeholder="Optional feedback" ${p.resultPublished?'disabled':''}></div></div></div>`).join(""):`<div class="empty">No written questions in this examination.</div>`}</div>${!p.resultPublished?`<div class="card"><div class="section-head"><div><h2>Finalize Result</h2><p class="mini">Save marks first. Then publish only after every submitted written answer has been checked.</p></div></div><button class="btn" onclick="saveGrade(${p.attemptId})">Save Marks</button> <button class="btn success" onclick="publishResult(${p.attemptId})">Publish Result</button></div>`:`<div class="card"><div class="section-head"><div><h2>Result Published</h2><p class="mini">The student can now see the result in the Student Portal.</p></div><button class="btn" onclick="teacher()">Back to Dashboard</button></div></div>`}<button class="btn ghost" onclick="paperReview()">← Back to Papers</button><div class="footer">Concept Classes · Designed & Developed by Yash Pandey</div></div>`}catch(e){alert(e.message)}}
async function saveGrade(id){const items=[...document.querySelectorAll('.grade-mark')].map(el=>({questionId:Number(el.dataset.qid),marks:Number(el.value||0),comment:document.querySelector(`.grade-comment[data-qid="${el.dataset.qid}"]`)?.value||""}));try{await api(`/api/admin/papers/${id}/grade`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({items})});alert("Marks saved successfully.");reviewAttempt(id)}catch(e){alert(e.message)}}
async function publishResult(id){if(!confirm("Publish this result? The student will be able to view it."))return;try{await api(`/api/admin/results/${id}/publish`,{method:"POST"});alert("Result published successfully.");reviewAttempt(id)}catch(e){alert(e.message)}}
function toggleQ(){document.getElementById("opts").style.display=document.getElementById("qt").value==="written"?"none":"block"}
async function mkStudent(){try{await api("/api/admin/student",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:document.getElementById("su").value.trim(),password:document.getElementById("sp").value})});alert("Student account created successfully.");teacher()}catch(e){alert(e.message)}}
async function mkTest(){try{await api("/api/admin/tests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:document.getElementById("tt").value,subject:document.getElementById("sub").value,className:document.getElementById("cl").value,duration:document.getElementById("dur").value})});teacher()}catch(e){alert(e.message)}}
async function addQ(){try{const testId=document.getElementById("tsel")?.value;if(!testId)return alert("Please select an examination first.");const question=document.getElementById("qq")?.value.trim();if(!question)return alert("Please enter the question.");let body={question,type:document.getElementById("qt").value,a:document.getElementById("qa")?.value||"",b:document.getElementById("qb")?.value||"",c:document.getElementById("qc")?.value||"",d:document.getElementById("qd")?.value||"",correct:document.getElementById("cr")?.value||"",marks:document.getElementById("mk").value,negative:document.getElementById("neg").value,uploadWindow:document.getElementById("qt").value==="written"?document.getElementById("uw").value:0};await api(`/api/admin/tests/${testId}/questions`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});alert("Question added successfully.");teacher()}catch(e){alert(e.message)}}
async function student(){let[ts,results]=await Promise.all([api("/api/tests"),api("/api/student/results")]);app.innerHTML=nav("Student Portal")+`<div class="wrap"><div class="top-banner"><h1 class="hero-title">Welcome to your examination portal</h1><p class="muted" style="margin:0">Take assigned examinations and view your published results.</p></div><div class="card"><div class="section-head"><div><h2>My Results</h2><p class="mini">Results appear here after your teacher checks and publishes them.</p></div><span class="badge green">${results.length} published</span></div>${results.length?results.map(r=>`<div class="card" style="box-shadow:none;background:#fbfcff"><div class="section-head"><div><h3 style="margin-bottom:6px">${esc(r.title)}</h3><p class="mini">${esc(r.subject)} · Class ${esc(r.className)} · Published ${new Date(r.publishedAt).toLocaleDateString()}</p></div><div style="text-align:right"><div style="font-size:26px;font-weight:800">${r.score}/${r.total}</div><button class="btn ghost" onclick="viewStudentResult(${r.attemptId})">View Result</button> <button class="btn" onclick="downloadResult(${r.attemptId})">Download PDF</button></div></div></div>`).join(""):`<div class="empty">No results have been published for you yet.</div>`}</div><div class="card"><div class="section-head"><h2>Available Examinations</h2><span class="badge blue">${ts.length} available</span></div>${ts.length?ts.map(t=>`<div class="card" style="box-shadow:none;background:#fbfcff"><div class="section-head"><div><h3 style="margin-bottom:6px">${esc(t.title)}</h3><p class="mini">${esc(t.subject)} · Class ${esc(t.class_name)} · ${t.duration} minutes</p></div><button class="btn" onclick="openTest(${t.id})">Start Examination →</button></div></div>`).join(""):`<div class="empty">No examinations are currently assigned to you.</div>`}</div><div class="footer">Concept Classes · Secure Examination Portal · Designed & Developed by Yash Pandey</div></div>`}
async function viewStudentResult(id){let rs=await api('/api/student/results');let r=rs.find(x=>x.attemptId===id);if(!r)return alert('Result not found');app.innerHTML=nav("My Result")+`<div class="wrap"><div class="top-banner"><h1 class="hero-title">${esc(r.title)}</h1><p class="muted">Result declared on ${new Date(r.publishedAt).toLocaleString()}</p></div><div class="stats"><div class="stat"><div class="stat-label">Total Score</div><div class="stat-value">${r.score}/${r.total}</div></div><div class="stat"><div class="stat-label">MCQ Score</div><div class="stat-value">${r.mcqScore}</div></div><div class="stat"><div class="stat-label">Written Score</div><div class="stat-value">${r.writtenScore}</div></div></div><div class="card"><div class="section-head"><h2>Question-wise Result</h2><span class="badge green">Result Declared</span></div>${r.questions.map((q,i)=>`<div class="paper-item"><div class="question-number">Question ${i+1} · ${q.type==='written'?'Written':'MCQ'}</div><div class="question-title">${esc(q.question)}</div><p><b>${q.awardedMarks}/${q.maxMarks} marks</b></p>${q.comment?`<div class="mini"><b>Teacher comment:</b> ${esc(q.comment)}</div>`:''}</div>`).join('')}</div><button class="btn" onclick="downloadResult(${r.attemptId})">Download Result PDF</button> <button class="btn ghost" onclick="student()">← Back to Student Portal</button><div class="footer">Concept Classes · Designed & Developed by Yash Pandey</div></div>`}
async function downloadResult(id){let rs=await api('/api/student/results'),r=rs.find(x=>x.attemptId===id);if(!r)return alert('Result not found');if(!window.jspdf?.jsPDF){window.print();return}const {jsPDF}=window.jspdf;const doc=new jsPDF();let y=20;doc.setFontSize(20);doc.text('CONCEPT CLASSES',20,y);y+=10;doc.setFontSize(14);doc.text('Examination Result',20,y);y+=10;doc.setFontSize(11);doc.text(`Examination: ${r.title}`,20,y);y+=7;doc.text(`Subject: ${r.subject} | Class: ${r.className}`,20,y);y+=7;doc.text(`Score: ${r.score}/${r.total}`,20,y);y+=12;doc.setFontSize(12);doc.text('Question-wise Summary',20,y);y+=8;doc.setFontSize(10);r.questions.forEach((q,i)=>{let lines=doc.splitTextToSize(`${i+1}. ${q.question}`,165);if(y+lines.length*5+12>280){doc.addPage();y=20}doc.text(lines,20,y);y+=lines.length*5;doc.text(`Marks: ${q.awardedMarks}/${q.maxMarks}${q.comment?' | Comment: '+q.comment:''}`,25,y);y+=8});doc.setFontSize(9);doc.text('Designed & Developed by Yash Pandey',20,290);doc.save(`Concept-Classes-Result-${id}.pdf`)}
async function openTest(id){let d=await api("/api/tests/"+id);S.test=d.test;S.questions=d.questions;S.violations=0;S.submitted=false;let a=await api("/api/attempts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({testId:id})});S.attemptId=a.attemptId;app.innerHTML=nav(S.test.title)+`<div class="wrap"><div class="top-banner"><b>Examination in progress</b><div class="mini" style="margin-top:4px">Keep your face visible and remain on this exam window. Three proctoring violations will submit the examination automatically.</div></div><div class="grid"><main class="card"><div id="qs"></div><div class="exam-actions"><button class="btn danger" onclick="submitExam()">Submit Examination</button></div></main><aside class="card sticky-side"><h3 style="margin-bottom:5px">Secure Proctoring</h3><p class="mini">Camera and face detection are active during the examination.</p><div class="camera"><video id="video" autoplay muted playsinline></video></div><div class="proctor-status"><span class="status-dot"></span><span id="fs" class="mini">Starting camera...</span></div><p style="margin-bottom:4px">Violations <b id="vc">0</b> / 3</p><div class="timer" id="tm">${String(a.duration).padStart(2,"0")}:00</div></aside></div><div class="footer">Concept Classes · Designed & Developed by Yash Pandey</div></div>`;document.getElementById("qs").innerHTML=S.questions.map((q,i)=>q.type==="written"?`<div class="question"><div class="question-head"><div><div class="question-number">Question ${i+1} · Written</div><div class="question-title">${esc(q.question)}</div></div><span class="badge amber">${q.marks} marks</span></div><div class="written-upload"><b>Written solution required</b><p class="mini">Solve in your notebook, take a clear photo and upload it.</p><p class="mini">Upload deadline: <b>${q.upload_window} minutes from exam start</b></p><input type="file" accept="image/jpeg,image/png,image/webp" id="file${q.id}" onchange="uploadWritten(${q.id})"><span id="up${q.id}" class="mini">No image uploaded</span></div></div>`:`<div class="question"><div class="question-head"><div><div class="question-number">Question ${i+1} · MCQ</div><div class="question-title">${esc(q.question)}</div></div><span class="badge blue">${q.marks} marks</span></div>${["A","B","C","D"].map(x=>`<label><input type="radio" name="q${q.id}" value="${x}"> <b>${x}.</b> ${esc(q["option_"+x.toLowerCase()])}</label>`).join("")}</div>`).join("");proctor();countdown(a.duration*60)}
async function uploadWritten(qid){let f=document.getElementById("file"+qid).files[0];if(!f)return;let fd=new FormData();fd.append("questionId",qid);fd.append("image",f);try{let r=await fetch(`/api/attempts/${S.attemptId}/answer`,{method:"POST",body:fd}),d=await r.json();if(!r.ok)throw Error(d.error);document.getElementById("up"+qid).textContent="✓ Solution image uploaded successfully"}catch(e){alert(e.message)}}
async function proctor(){try{await faceapi.nets.tinyFaceDetector.loadFromUri("https://justadudewhohacks.github.io/face-api.js/models");let st=await navigator.mediaDevices.getUserMedia({video:true,audio:false}),v=document.getElementById("video");v.srcObject=st;S.monitor=setInterval(async()=>{if(S.submitted)return;let d=await faceapi.detectAllFaces(v,new faceapi.TinyFaceDetectorOptions({inputSize:320,scoreThreshold:.5}));document.getElementById("fs").textContent=d.length===1?"Camera active · One face detected":d.length===0?"No face detected":"Multiple faces detected";if(d.length!==1)violate(d.length===0?"No face detected":"Multiple faces detected")},1800)}catch(e){document.getElementById("fs").textContent="Camera unavailable — check browser permissions"}}
function violate(m){if(S.submitted)return;S.violations++;let vc=document.getElementById("vc");if(vc)vc.textContent=S.violations;api(`/api/attempts/${S.attemptId}/event`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type:"proctor",message:m})});if(S.violations>=3)submitExam()}
document.addEventListener("visibilitychange",()=>{if(S.attemptId&&!S.submitted&&document.hidden)violate("Tab/window switched or hidden")});window.addEventListener("blur",()=>{if(S.attemptId&&!S.submitted)violate("Exam window lost focus")});
function countdown(sec){let x=sec;clearInterval(S.timer);S.timer=setInterval(()=>{x--;let el=document.getElementById("tm");if(el)el.textContent=`${String(Math.floor(x/60)).padStart(2,"0")}:${String(x%60).padStart(2,"0")}`;if(x<=0)submitExam()},1000)}
async function submitExam(){if(S.submitted)return;S.submitted=true;clearInterval(S.monitor);clearInterval(S.timer);let answers={};S.questions.filter(q=>q.type==="mcq").forEach(q=>{let x=document.querySelector(`input[name="q${q.id}"]:checked`);if(x)answers[q.id]=x.value});try{let r=await api(`/api/attempts/${S.attemptId}/submit`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({answers})});app.innerHTML=`<div class="login-shell" style="min-height:100vh"><section class="login-panel" style="grid-column:1/-1"><div class="login-box" style="text-align:center"><div style="font-size:52px">✓</div><h2>Examination Submitted</h2><p class="muted">Your examination has been submitted successfully.</p><div class="card" style="box-shadow:none;background:#f8fafc;margin-top:22px"><div style="font-size:34px;margin-bottom:8px">📩</div><h3 style="margin:0 0 8px">Result will be declared soon</h3><p class="muted" style="margin:0">You will receive your result in your class group once it has been checked and declared by your teacher.</p><div class="mini" style="margin-top:14px">Your submission and proctoring record have been saved.</div></div><button class="btn" onclick="student()">Back to Examinations</button><div class="login-footer">Concept Classes · Designed & Developed by Yash Pandey</div></div></section></div>`}catch(e){S.submitted=false;alert(e.message)}}
boot();
